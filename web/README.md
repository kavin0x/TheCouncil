# TheCouncil web

Next.js dashboard and marketing for TheCouncil. See the [repository root README](../README.md) for layout and how the API fits in.

## Commands

```bash
npm ci
npm run dev
```

Other scripts: `npm run build`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e`.
