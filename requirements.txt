openai>=1.51.0
pyyaml>=6.0.2
python-dotenv>=1.0.1
pynput>=1.7.0
rich>=13.9.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
stripe>=10.0.0
pytest>=7.0.0
pytest-asyncio>=0.23.0
mcp>=1.26.0
fastmcp>=0.2.0
e2b>=2.19.0

# Desktop sandbox (computer-use agent loop, Ultra+ only)
e2b-desktop>=2.3.0

# Web search via Tavily (Pro+ only)
tavily-python>=0.7.23

# Database (PostgreSQL async)
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0

# Cache / message bus (Redis Streams)
redis[asyncio]>=5.0.0

# Background task orchestration (Celery + Redis broker)
celery[redis]>=5.4.0

# Typer-based CLI
typer[all]>=0.12.0

# Production-ready enhancements
alembic>=1.13.0
slowapi>=0.1.9
python-multipart>=0.0.6
structlog>=24.1.0
python-json-logger>=2.0.7
cryptography>=42.0.0
PyJWT>=2.8.0
