openai>=3.8.0
pyyaml>=6.0.2
python-dotenv>=1.2.3
pynput>=1.8.2
rich>=15.0.0
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
httpx>=0.28.1
pytest>=9.0.3
pytest-asyncio>=0.23.0
mcp>=1.26.0
fastmcp>=4.0.2

# Docker support for sandboxing
docker>=7.1.0

# Web search via Tavily (optional)
tavily-python>=0.7.26

# Database (PostgreSQL async + SQLite)
asyncpg>=0.31.0
aiosqlite>=0.22.1
sqlalchemy[asyncio]>=2.0.0

# Cache / message bus (Redis Streams) + Celery broker.
# No published celery[redis] extra accepts redis 8 (PyPI, 2026-09-07):
#   celery 5.4.0 extra: redis!=4.5.5,<6.0.0,>=4.5.2
#   celery 5.6.3 extra: kombu[redis] → redis!=4.5.5,!=5.0.2,<6.5,>=4.5.2
# Pin celery[redis] to the 5.6 line and match kombu[redis] 5.6.2.
celery[redis]>=5.6.3
redis>=4.5.2,<6.5,!=4.5.5,!=5.0.2

# Typer-based CLI
typer[all]>=0.27.2

# Production-ready enhancements
alembic>=1.18.5
slowapi>=0.1.10
python-multipart>=0.0.30
structlog>=26.1.0
python-json-logger>=4.1.0
cryptography>=42.0.0
PyJWT>=2.8.0
